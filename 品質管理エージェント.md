# 品質管理エージェント

## 役割

実装結果が承認済み範囲、完了条件、プロジェクト規則を満たすかを検証し、品質ゲートをPASSまたはFAILとして判定する。実行環境、フェーズ品質ゲート、DB変更、E2Eの正式な手順は[`品質管理.md`](品質管理.md)を正本とする。

メインが`gpt-6-astra`または`gpt-5.6-sol`の場合、品質管理とPASS / FAIL判定はメインエージェント自身が担当する。実装サブエージェントへ品質判定を委譲しない。実装委譲からの復帰とFAIL時のループは[`MULTI_AGENT_WORKFLOW.md`](MULTI_AGENT_WORKFLOW.md)に従う。

品質管理中は原則として実装を修正しない。失敗内容、再現方法、該当する規則を実装エージェントへ差し戻す。テスト期待値の明白な誤記などを修正する必要がある場合も、実装範囲として扱ってから再検証する。

## 受け入れ確認

- 承認済み計画または小規模変更の要求と、実際の差分を照合する。
- 各完了条件を、コード、テスト、実行結果のいずれで確認したか明確にする。
- 計画外のコード、設定、依存、DB、生成物、文書変更がないことを確認する。
- ユーザーの既存差分が削除・上書きされていないことを確認する。
- 検証未実施の項目を推測でPASSにしない。

## 実行環境

- 通常のformat、typecheck、Unit Testは`compose.dev.yml`のapplicationサービスで実行する。
- DB統合テストは`compose.test.yml`を使用する。
- Node.js、pnpm、TypeScript、import境界の機械判定は`pnpm typecheck`に含まれるプロジェクトルール検査に従う。
- E2EはホストのNode.jsとPlaywright Chromiumを使用し、正式な入口である`./tools/run-e2e`から実行する。
- `compose.dev.yml`または`compose.test.yml`内でPlaywrightを実行しない。
- 実行環境を用意できない場合はPASSにせず、理由と必要な対応を報告する。

## テスト選択

- 変更した振る舞いを直接検証する最小のテストレベルを選び、必要なら上位レベルを追加する。
- 副作用のない関数、FrontendのAPI Client、Session、Query、Hookの分離可能なロジックはUnit Testで検証する。
- BackendのHTTP status、Request検証、Response形式はRouter Testで検証する。
- 権限、状態遷移、ビジネスルールはService Testで検証する。
- SQL、制約、Transaction、migration、Repository間連携はIntegration Testで検証する。
- 認証、Session境界、主要ユーザーフロー、ブラウザ固有動作はE2Eで検証する。
- Request・Responseの推論や禁止した入力は、typecheckで評価できる型テストを使用する。
- 不具合修正には、修正前の不具合を再現するRegression Testがあることを確認する。

## テスト規則

- テストは内部実装ではなく、入力、出力、HTTP status、永続化、副作用を検証する。
- テストファイルは原則として対象ファイルの近くへ`*.test.ts`または`*.test.tsx`として配置する。
- 実DB接続が必要なテストだけを`*.integration.test.ts`にする。
- Unit Testから実DBへ接続しない。
- Mockはテスト対象の外部境界に限定する。
- `as never`で不完全なMockを強制注入していないか確認し、必要なら型付きfactoryや必要最小限のinterfaceを要求する。
- テストに`.only`や意図しない`.skip`が残っていないことを確認する。
- 実装に合わせて期待値を無条件に書き換え、Regressionを隠していないことを確認する。

## フェーズ品質ゲート

各フェーズで次の順序を守る。

1. 現在Phaseの差分と完了条件を確認する。
2. `compose.dev.yml`のapplicationサービスで`pnpm format`を実行する。
3. 同サービスで`pnpm verify:phase`を実行する。
4. typecheckまたはtestが失敗した場合は、原因と再現方法を実装エージェントへ差し戻す。
5. 修正後は失敗したコマンドだけでなく、`pnpm verify:phase`を最初から再実行する。
6. typecheckとtestが両方成功した場合だけ、PhaseをPASSとする。
7. 計画に必要なIntegration TestまたはE2Eを実行する。
8. 現在Phaseの全ゲートがPASSした後だけ、次Phaseへの移行を認める。

全Phase完了後にも`pnpm verify:phase`を実行し、全体回帰がないことを確認する。

## DB変更の検証

DBモデル変更がある場合は、通常ゲートの前に次を確認する。

1. `pnpm db:generate`でmigrationが生成されている。
2. 生成されたSQLに意図しないDROP、データ消失、制約変更がない。
3. 接続先がローカル開発DBである。
4. `pnpm db:migrate`が成功する。
5. `compose.test.yml`でDB Integration Testが成功する。

- DBモデル変更がない場合はmigrationを実行しない。
- migrationを手書きした場合は、その必要性と生成方式を確認する。
- データ消失の可能性がある変更は、計画内の承認と復旧方法を確認する。

## E2E

- E2Eが必要な変更では、ホストから`./tools/run-e2e`を実行する。
- `compose.e2e.yml`はE2E用DBの起動専用とし、Playwright、Backend、Viteの実行先にしない。
- E2E用にDockerfile、Composeサービス、Playwright用イメージを追加しない。
- 失敗時にもE2E用DBが停止される正式なrunnerを迂回しない。
- E2Eを実行しない場合は、変更内容に対して不要と判断した根拠を報告する。

## 静的・機械検査

既存の`pnpm typecheck`に含まれる検査に加え、差分を次の観点で確認する。

- `any`、`@ts-ignore`、型検査回避の`as never`、二重キャストが追加されていない。
- Applicationコードで未処理Promiseが追加されていない。
- Frontend Component、Hook、Contextから`fetch`が直接呼ばれていない。
- Token用`localStorage`へ`sessionManager`とテスト以外からアクセスしていない。
- Backendのconfig、entry point、テスト以外から`process.env`を直接参照していない。
- Frontend、Backend、packagesのimport境界を破っていない。
- `packages`にNode専用またはDOM専用コードを持ち込んでいない。
- API日時へ意図しないUTC変換や末尾`Z`を追加していない。
- `.only`、意図しない`.skip`、デバッグログ、秘密情報が残っていない。

繰り返し発生する違反は、`tools/check-project-rules.ts`などで機械検査へ追加する提案を行う。機械検査自体の変更は、承認された実装範囲に含まれる場合だけ行う。

## 判定と差し戻し

PASS報告には次を含める。

- 確認した完了条件
- 実行したコマンドと結果
- 実行したUnit、Integration、E2Eの範囲
- DB、依存、設定、生成物の確認結果
- 未実行項目がないこと、または不要と判断した根拠

FAIL報告には次を含める。

- 失敗した完了条件またはコマンド
- 再現方法
- 期待値と実際の結果
- 該当ファイルまたは責務
- 実装エージェントに必要な修正内容

高モデル時は同じ種類の実装サブエージェントへ上記を差し戻す。書き込み役を同時に複数起動せず、修正完了後はメインが必要な品質ゲートを最初から再実行する。

FAIL後の修正が完了したら、影響範囲に応じた品質ゲートを最初から再実行する。

全フェーズ完了・最終PASS後は、上記の報告を[`PR作成エージェント.md`](PR作成エージェント.md)へ引き継ぐ。機械入力の形式は[`codx/README.md`](codx/README.md)に従い、公開モード、head、対象commit SHA、reviewBaseSha、完了した開始記録の参照と実行証跡を記録する。PR時はタスク差分とPR全diffの範囲を区別する。版branchにPR baseやPR URLを要求しない。
