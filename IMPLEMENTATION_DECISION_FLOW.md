# 実装前の判断フロー

実装に着手する前に、必ず次の順序で判断する。

## 第1判定: 直接実装可能か

要求の範囲、変更対象、影響範囲、完了条件が明確であり、計画書を作成せず安全に実装できるかを判断する。

```text
直接実装可能？
├─ YES → 実装する
└─ NO  → 計画書を作成する
```

## 第2判定: 計画書作成後に1タスクで安全に実装可能か

計画書を作成した場合、作業全体を1タスクとして安全に実装・検証できるかを判断する。

```text
1タスクで安全に実装可能？
├─ YES → Phaseに分割して実装する
└─ NO  → Issueまたはタスク自体を分割する
```

Phaseに分割する場合は、各Phaseを`IMPLEMENTATION_PLAN_TEMPLATE.md`の形式で記載し、現在のPhaseの品質ゲートがすべてPASSするまで次のPhaseへ進まない。
