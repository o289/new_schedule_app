# 実装前の判断フロー

実装依頼を受けた場合、コード変更に着手する前に、必ず以下の順序で判断する。

## 第1判定：直接実装可能か

要求の範囲、変更対象、影響範囲、完了条件を確認し、計画書を作成せず安全に実装できるかを判断する。

```text
直接実装可能？
├─ YES → 小規模変更として、そのまま実装する
└─ NO  → 中規模以上として、実装計画書を作成する
```

### 小規模変更の場合

直接実装可能と判断した場合、Phase分割および実装計画書の作成は行わない。

以下の順序で実装・検証する。

```text
実装
 ↓
typecheck
 ↓
成功？
├─ NO → 原因を分析して修正
│        ↓
│      typecheckを再実行
│
└─ YES
     ↓
    test
     ↓
    成功？
    ├─ NO → 原因を分析して修正
    │        ↓
    │      typecheckから再実行
    │
    └─ YES
         ↓
        完了
```

typecheckとtestが両方PASSするまで、実装完了として扱わない。

---

## 第2判定：中規模以上の変更

直接実装できないと判断した場合は、コード・設定・DB・生成物を変更せず、先に実装計画書を作成する。

実装計画は `docs/実装計画` ディレクトリに配置する。

新規機能追加の場合は、計画書の作成前にUIのモック画像をPNG形式で作成する。モック画像の保存先と目的を計画書に記載する。

```text
直接実装不可
 ↓
実装計画書を作成
 ↓
ユーザーへ提出
 ↓
停止
 ↓
ユーザーから明示的な実装承認を待つ
```

ユーザーの実装承認を受けるまで、コード・設定・DB・生成物を変更してはならない。

---

## 第3判定：計画を1タスクで安全に実装可能か

ユーザーから実装承認を受けた後、計画全体を1つのタスクとして安全に実装・検証できるかを判断する。

ユーザーによる実装計画の承認は、その`*_実装計画.md`に記載された全Phaseへの承認とする。Phase間で追加承認を求めない。

```text
1タスクで安全に実装可能？
├─ YES → Phaseに分割して実装する
└─ NO  → Issueまたはタスク自体を分割する
```

### 1タスクで安全に実装可能な場合

実装計画を複数のPhaseに分割する。

各Phaseは `IMPLEMENTATION_PLAN_TEMPLATE.md` の形式に従って記載する。

各Phaseについて、以下のループを実行する。

```text
Phase 1
 ↓
実装
 ↓
必要な場合はDB migration
 ↓
typecheck
 ↓
成功？
├─ NO → 原因分析 → 修正 → typecheck再実行
└─ YES
     ↓
    test
     ↓
    成功？
    ├─ NO → 原因分析 → 修正
    │        ↓
    │      typecheckから再実行
    │
    └─ YES
         ↓
      Phase 1 PASS
         ↓
      Phase 2へ
```

現在のPhaseの品質ゲートがすべてPASSするまで、次のPhaseへ進んではならない。

すべてのPhaseについて同じループを繰り返す。

Phaseの品質ゲートがPASSしたら、同じ計画書の次のPhaseへ自動的に進む。新たな権限、計画外変更、仕様上の判断が必要になった場合を除き、Phase移行だけを理由に停止または追加承認を求めない。

---

## 1タスクで安全に実装できない場合

計画全体を1タスクとして実装せず、Issueまたは独立したタスクへ分割する。

分割した各Issueまたはタスクには、それぞれ専用の`*_実装計画.md`を作成する。複数の独立タスクを1つの実装計画書へPhaseとして混在させない。

各計画書の承認範囲は、その計画書に記載された全Phaseである。別の計画書へ移る場合は、その計画書が承認済みかを確認する。

```text
大きな実装計画
 ↓
1タスクでは安全に実装できない
 ↓
Issue / タスクへ分割
 ↓
各Issue / タスクを独立して実装
 ↓
それぞれで
実装 → typecheck → test → PASS
```

分割後の各Issueまたはタスクについても、この判断フローを適用する。

---

# 全体フロー

```text
「実装して」
    ↓
第1判定
直接実装可能？
    │
    ├─ YES【小規模】
    │     ↓
    │   そのまま実装
    │     ↓
    │   typecheck
    │     ↓
    │   test
    │     ↓
    │   完了
    │
    └─ NO【中規模以上】
          ↓
        計画書作成
          ↓
        ユーザー承認
          ↓
        1タスクで安全に実装可能？
          │
          ├─ YES
          │    ↓
          │  Phase分割
          │    ↓
          │  Phase 1
          │  → 実装
          │  → 必要ならDB migration
          │  → typecheck
          │  → test
          │  → PASS
          │    ↓
          │  Phase 2
          │  → 実装
          │  → 必要ならDB migration
          │  → typecheck
          │  → test
          │  → PASS
          │    ↓
          │  ...
          │    ↓
          │  全Phase完了
          │
          └─ NO
               ↓
             Issue / タスク分割
               ↓
             分割した各タスクに
             本判断フローを適用
```

## 原則

- 小規模変更では実装計画書を作成しない。
- 小規模変更ではPhase分割しない。
- 中規模以上の変更は、実装前に実装計画書を作成する。
- 中規模以上では、ユーザーの明示的な承認を得るまで実装を開始しない。
- 承認後、1タスクで安全に実装可能ならPhase単位で実装する。
- 同じ実装計画書内では、Phase完了ごとの追加承認を求めず、品質ゲートPASS後に次のPhaseへ進む。
- 1タスクで安全に実装できない場合はIssueまたはタスク自体を分割し、タスクごとに専用の`*_実装計画.md`を作成する。
- 各Phaseでは、品質ゲートがPASSするまで次のPhaseへ進まない。
- typecheckが失敗した状態でtestへ進まない。
- test失敗後にコードを修正した場合は、typecheckから検証をやり直す。
- DB変更が必要な場合は、そのPhase内で必要なmigration処理を行ってから検証する。
- typecheckとtestが成功していない状態を実装完了として扱わない。
