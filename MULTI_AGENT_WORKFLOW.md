# メイン・実装サブエージェント運用

## 適用条件

このフローは、メインエージェントが`gpt-6-astra`または`gpt-5.6-sol`で実行されている開発タスクに適用する。メインモデルはユーザーがCodexのモデル選択で指定する。プロジェクト文書はメインモデル自体を変更しない。

適用条件を満たさない場合は、各役割文書に従って現在のエージェントが工程ごとに役割を切り替える。現在のモデルを確認できない場合は、モデル名を推測して委譲しない。

## 責務

- メインエージェントは要求整理、調査、規模判定、実装計画、ユーザー承認の確認、品質管理、公開工程への引き継ぎを担当する。
- 実装サブエージェントは、承認済み計画または小規模変更の要求から指定された現在Phaseだけを実装し、事実をメインへ返す。
- 小規模変更は`implementation_luna`、中・大規模変更は`implementation_terra`へ委譲する。
- 品質ゲートのPASS / FAILはメインエージェントだけが判定する。実装サブエージェントは自己確認結果を品質PASSとして扱わない。
- PR作成エージェントへの移行とbranch別公開は、最終品質PASS後にメインエージェントが行う。

## 同時実行の禁止

- 書き込み権限を持つ実装サブエージェントは、常に1つだけ起動する。
- 実装中に別の書き込みサブエージェントを起動しない。
- メインエージェントは、実装サブエージェントの実行中にコード、設定、DB、migration、生成物を並行変更しない。
- 複数PhaseやFrontend / Backendに分かれる場合も、書き込みタスクを並列化せず順番に委譲する。
- `.codex/config.toml`の`max_concurrent_threads_per_session = 1`を機械的な上限とする。

## 実装サブエージェントへの入力

メインエージェントは次を一つの委譲メッセージに含める。

- 承認済み計画または小規模変更の要求
- 現在のPhase、変更対象、対象外、完了条件
- 維持する既存契約とユーザーフロー
- 作業branchと開始記録
- DB、依存、設定、生成物の変更可否
- 追加・更新すべきテスト
- 実装後に返す引き継ぎ形式

情報が不足している場合は委譲せず、計画作成工程へ戻す。

## 実装から品質管理への復帰

実装サブエージェントの完了後、メインエージェントは結果だけを信用せず、実際の差分を確認する。計画外変更、未解決事項、ユーザー差分との競合があれば品質ゲートへ進まず差し戻す。

差分が現在Phaseと一致する場合、メインエージェントが[品質管理エージェント.md](品質管理エージェント.md)へ役割を切り替え、正式環境で品質ゲートを実行する。

```text
メイン: 計画・現在Phaseを確定
  ↓
Luna / Terra: 現在Phaseだけ実装
  ↓
メイン: 差分確認
  ↓
メイン: format → typecheck → test → 必要なIntegration / E2E
  ↓
PASS?
├─ NO → 同じ実装役へ原因・再現方法・修正範囲を差し戻す
│        ↓
│      修正完了後、メインが品質ゲートを最初から再実行
└─ YES → 次Phaseを1つだけ委譲、または最終品質確認へ進む
```

## FAIL時のループ

- メインエージェントは、失敗コマンド、期待値、実際の結果、再現方法、修正可能範囲を同じ実装サブエージェントへ返す。
- サブエージェントが完了して再利用できない場合だけ、同じ種類の実装サブエージェントを1つ起動し、元の実装引き継ぎと失敗情報を渡す。
- 修正中も書き込みサブエージェントは1つに限定する。
- 修正後のPASS判定はサブエージェントへ委譲せず、メインエージェントが必要な品質ゲートを最初から再実行する。
- 仕様判断、計画外変更、新しい権限が必要な場合は自動修正を続けず、ユーザーへ報告する。

## サブエージェントの完了報告

実装サブエージェントは次を返す。

- 変更ファイル
- 実装内容
- 追加・更新したテスト
- DB、migration、依存、設定、生成物の変更有無
- 自己確認として実行したコマンドと結果
- 未実行の検証
- 未解決事項と計画外変更の必要性

サブエージェントはpush、PR作成、Merge、branch削除を行わない。
