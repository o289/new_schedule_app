#!/bin/sh
set -eu
if [ "$#" -ne 2 ]; then
  echo 'STOP: agent run <command> <runId>形式だけを受け付けます。' >&2
  exit 1
fi
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/../.." && pwd -P)
cd "$repo_root"
pnpm runtime:check
exec pnpm exec tsx .agents/tools/agent-run-command.ts "$@"
