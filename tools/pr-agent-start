#!/bin/sh
set -eu
if [ "$#" -ne 0 ]; then
  echo 'STOP: pr-agent-start accepts no arguments.' >&2
  exit 1
fi
repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd -P)
cd "$repo_root"
exec pnpm exec tsx tools/pr-agent-start.ts
