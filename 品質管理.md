# 品質管理

## E2Eテスト実行環境

- E2EはホストのNode.js `v22.23.1`と、ホストに導入したPlaywright Chromiumで実行する。
- `compose.e2e.yml`はE2Eテスト用DBの起動専用であり、Playwright・backend・Viteの実行先として使用しない。
- `compose.dev.yml`および`compose.test.yml`のサービス内で`pnpm test:e2e`を実行しない。
- E2EテストのためにDockerfile、Composeサービス、Playwright用イメージを追加・ビルドしない。
- ローカルE2Eの正式な実行入口は`./tools/run-e2e`とする。
- `run-e2e`は`.nvmrc`のNode.jsバージョンを確認し、不一致時は利用可能なNVMで`nvm use`を試行する。切替不能時は明確なエラーで終了する。
- `run-e2e`は`.env.e2e`を確認してE2E用PostgreSQLを起動し、Playwright終了後は成功・失敗にかかわらずPostgreSQLを停止する。

```text
./tools/run-e2e
```

## フェーズ品質ゲート

各フェーズは次の順序で進める。

1. 現在のフェーズだけを実装する。
2. `pnpm format`を実行する。
3. `pnpm verify:phase`を実行する。
4. typecheckが失敗した場合は原因を分析して修正し、`pnpm verify:phase`を再実行する。
5. testが失敗した場合は原因を分析して修正し、testだけでなく`pnpm verify:phase`を再実行する。
6. typecheckとtestの両方が成功した場合のみ、そのフェーズを完了にする。
7. 次のフェーズへ進む。

すべてのフェーズ完了後にも`pnpm verify:phase`を実行し、全体の回帰がないことを確認する。

検証を実行できない場合はフェーズを完了扱いにせず、実行できない理由と必要な対応を報告する。

## DB変更を含むフェーズ

DBモデルを変更した場合は、品質ゲートの前に以下を行う。

1. `pnpm db:generate`でmigrationを生成する。
2. 生成されたSQLを確認する。
3. 接続先がローカル開発DBであることを確認する。
4. `pnpm db:migrate`を実行する。
5. 通常の品質ゲートに加えて、`compose.test.yml`でDB統合テストを実行する。

DBモデルの変更がない場合はmigrationを実行しない。
